# Your task

# Complete these steps:

    Log in to Azure.
    Create a resource group (if needed).
    Create an Azure Container Registry.
    Log in to ACR.
    Tag your Docker image.
    Push the image.
    Verify it appears in ACR using both the Azure CLI and the Azure Portal.


# Build Docker Image

a. Get ACR Login Server
az acr show --name akspracticeacr26abin --query loginServer --output tsv

b. login to ACR
az acr login --name akspracticeacr26abin
az acr login -n akspracticeacr26abin --expose-token

c. Build Image
docker build -t backend:v1 .

Delete image if required after use:
docker rmi backend:v1


d. Tag image
docker tag backend:v1 akspracticeacr26abin.azurecr.io/backend:v1
docker tag hello-fastapi:v1 akspracticeacr26abin.azurecr.io/hello-fastapi:v1


e. Push Image
# make sure you login before pushing
az acr login --name akspracticeacr26abin
docker push akspracticeacr26abin.azurecr.io/backend:v1
docker push akspracticeacr26abin.azurecr.io/hello-fastapi:v1

f. verify image exist in ACR
az acr repository list --name akspracticeacr26abin --output table

g. Delete image from ACR repository
az acr repository delete --name akspracticeacr26abin --image azure-rag:v1

